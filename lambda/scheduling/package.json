{
  "name": "@scoringames/'$dir'",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.645.0",
    "@aws-sdk/lib-dynamodb": "^3.645.0",
    "@aws-sdk/client-eventbridge": "^3.645.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
